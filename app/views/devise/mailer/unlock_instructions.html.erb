<%
  @token    = object0 if object0.present? rescue nil
  @username = object1 if object1.present? rescue nil
  @resource = User.find_by_username(@username) rescue nil
%>

<p><%= t :hola %> <%= @username rescue nil %>!</p>

<p><%= t :su_cuenta_bloqueada %></p>

<p><%= t :haga_clic_desbloquear %></p>

<p><%= link_to t(:desbloquear_mi_cuenta), url_for(controller: 'devise/unlocks', action: 'show', unlock_token: @token, host: 'https://appasearesp.com') %></p>
