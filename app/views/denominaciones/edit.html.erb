<% title t(:edit_denomination) %>

<div class="box box-primary">
  <div class="box-header with-border">
    <h3 class="box-title"><%= t(:edit_denomination) %> — <%= @denominacion.nombre %></h3>
  </div>
  <div class="flash_messages"></div>
  <%= render 'form' %>
</div>
